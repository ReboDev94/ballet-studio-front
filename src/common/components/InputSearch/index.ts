import InputSearchComponent from './InputSearch';
import {
  IInputSearch as InputSearchProps,
  OptionInputSearch as IOptionInputSearch,
} from '../shared/interfaces/inputInterfaces';

const InputSearch = Object.assign(InputSearchComponent, {});

export default InputSearch;
export type { InputSearchProps, IOptionInputSearch };
